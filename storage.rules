rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && 
        (request.auth.uid == 'hx0LZRgjRQf28RTU060qs7posmd2' || 
         request.auth.token.email == 'pauloribeirosantos1606@gmail.com');
    }
  }
}
